<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AdminMapper">
    <select id="selectByUsername" resultType="com.example.entity.Admin">
        select id,username,password,nickname,avatar,phone,email,role,status,
        last_login_time as lastLoginTime,created_at as createAt 
        from admin where username=#{username};
    </select>
    <update id="updateLastLoginTime">
        update admin set last_login_time =NOW()
        where id=${id}
    </update>
    <select id="getById" resultType="com.example.entity.Admin">
        SELECT id, username, nickname, role, status
        FROM admin
        WHERE id = #{id}
    </select>
    <select id="selectAll" resultType="com.example.entity.Admin">
        SELECT username, nickname,avatar,phone, role, status,last_login_time as lastLoginTime,created_at as createAt
        from admin
    </select>
</mapper>