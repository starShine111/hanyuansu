<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <select id="selectAll" resultType="com.example.entity.User">
        select * from user
    </select>
    <insert id="insertUser" parameterType="com.example.entity.User" useGeneratedKeys="true">
        insert into user(username,password,name,avatar,role,gender,phone,account,email) values
        (#{username},#{password},#{name},#{avatar},#{role},#{gender},#{phone},#{account},#{email})
    </insert>
</mapper>